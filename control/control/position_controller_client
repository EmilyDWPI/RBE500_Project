from control.srv import control                                                     
import sys
import rclpy
from rclpy.node import Node


class MinimalClientAsync(Node):

    def __init__(self):
        super().__init__('control_client_async')
        
        self.cli = self.create_client(control, 'add_three_ints')       # CHANGE
        while not self.cli.wait_for_service(timeout_sec=1.0):
            self.get_logger().info('service not available, waiting again...')
        self.req = control.Request()                                   # CHANGE

    def send_request(self):
        self.req.joint_name = int(sys.argv[1])
        self.req.goal_theta = int(sys.argv[2])
        self.future = self.cli.call_async(self.req)


def main():
    rclpy.init()

    control_client = MinimalClientAsync()
    future = control_client.send_request()
    rclpy.spin_until_future_complete(control_client, future)
    response = future.result()
    control_client.get_logger().info(
        'Resultant effort: for joint: %d  and goal theta: %d is %d' %
        (int(sys.argv[1]), int(sys.argv[2]), response.effort))


    # while rclpy.ok():
    #     rclpy.spin_once(control_client)
    #     if control_client.future.done():
    #         try:
    #             response = control_client.future.result()
    #         except Exception as e:
    #             control_client.get_logger().info(
    #                 'Service call failed %r' % (e,))
    #         else:
    #             control_client.get_logger().info(
    #                 'Result of add_three_ints: for %d + %d + %d = %d' %                                # CHANGE
    #                 (control_client.req.a, control_client.req.b, control_client.req.c, response.sum))  # CHANGE
    #         break

    control_client.destroy_node()
    rclpy.shutdown()


if __name__ == '__main__':
    main()
